#include <stdio.h>

int main()

{
    int sum,num;
    do{
        printf("Enter a number");
        scanf("%d",&num);
        sum=sum+num;
    }while(num!=0);
    printf("%d",sum);

    return 0;
}
//do while loop or while loop is best as the program will iterate until a certain condition is met
