#include <stdio.h>

int main()
{
    int x;
    int sum;
    printf("Enter a number");
    scanf("%d",&x);
    int arr[x];
    for(int i=0;i<x;i++){
        printf("Enter elements");
        scanf("%d",&arr[i]);
        sum=sum+arr[i];
    }
    printf("%d",sum);

    return 0;
}
